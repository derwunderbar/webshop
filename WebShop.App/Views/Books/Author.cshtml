@model WebShop.Services.Models.AuthorDetails
@{
    ViewBag.Title = Model.Name;
}
<div id="content-container">
    <div class="horizontal">
        <img src="@Model.Avatar" alt="@Model.Name"/>
    </div>
    <div class="horizontal">
        <h3>@Model.Name</h3>
    </div>
</div>
