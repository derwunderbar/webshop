@model WebShop.ViewModels.Catalog.PagedEnumerableViewModel<WebShop.ViewModels.Catalog.BookViewModel>

<div>
    @Html.Partial("_BookListViewPartial", Model.Items)
</div>

<div class="page-buttons">
    <div>
        @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemsCount)
    </div>
</div>